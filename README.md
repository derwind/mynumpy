MyNumPy is an imitation of NumPy

It provides:

- a powerless N-dimensional array object
- unsophisticated (poor broadcasting) functions

Testing:

MyNumPy requires `pytest`.  Tests can then be run after installation with:

    pytest test
